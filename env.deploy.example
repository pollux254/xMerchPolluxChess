# Docker deploy credentials (do not commit this file)
# Recommend using a Docker Hub Personal Access Token.
DOCKER_USERNAME=your-dockerhub-username
DOCKER_PERSONAL_ACCESS_TOKEN=your-dockerhub-token
DOCKER_NAMESPACE=your-org-or-username # if you have no org, use your username

# Optional overrides
# DOCKER_REGISTRY=docker.io
# DEPLOY_TAG=latest

# ===================
# Build-time env (Next.js)
# ===================
# These are bundled into the image at build time
NEXT_PUBLIC_BASE_URL=https://xbase.yourdomain.tld       # Use your deployed domain (baked into build)
NEXT_PUBLIC_SUPABASE_URL=https://project.supabase.co    # Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key            # Supabase Dashboard → Settings → API

# Network (Xahau mainnet default)
NEXT_PUBLIC_XAHAU_NETWORK_ID=21337

# ===================
# Xaman / destination secrets
# ===================
# Do NOT place XUMM_API_KEY / XUMM_API_SECRET / XAH_DESTINATION here.
# Set them in Supabase Edge Function secrets instead (supabase secrets set ...).
# If you need a local fallback for testing, add them to your local .env.local only.
